import type { GridPosition } from '../grid/types';

export const BASIC_ENEMY_SPAWN: GridPosition = {
    col: 0,
    row: 0,
};

export const PLAYER_TOWER_SPAWN: GridPosition = {
    col: 4,
    row: 4,
};

export const LONG_RANGE_TOWER_SPAWN: GridPosition = {
    col: 2,
    row: 2,
};
